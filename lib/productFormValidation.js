exports.validateName = function (name) {
    if (!name) {
        return {
            field: 'nameErr',
            message: 'category must have name'
        }
    }
    return undefined
};

exports.validateCategory = function (category) {
    if (!slug) {
        return {
            field: 'categoryErr',
            message: 'must have category'
        }
    }
};
exports.validateSlug = function (slug) {
    if (!slug) {
        return {
            field: 'slugErr',
            message: 'category must have slug name'
        }
    }
};
exports.validateImage = function (slug) {
    if (!slug) {
        return {
            field: 'imageErr',
            message: 'category must have slug name'
        }
    }
};
exports.validateSlug = function (slug) {
    if (!slug) {
        return {
            field: 'slugErr',
            message: 'category must have slug name'
        }
    }
};
exports.validateSlug = function (slug) {
    if (!slug) {
        return {
            field: 'slugErr',
            message: 'category must have slug name'
        }
    }
};
exports.validateSlug = function (slug) {
    if (!slug) {
        return {
            field: 'slugErr',
            message: 'category must have slug name'
        }
    }
};